---
kind: pipeline
type: ssh
name: default

server:
  host: 10.0.1.142
  user: dev
  
clone:
  disable: true

steps:
- name: test
  commands:
  - mkdir hello
  - cd mkdir
  - touch world.txt

trigger:
  branch:
  - main
  event:
  - push
